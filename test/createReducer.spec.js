// import createReducer from '../src';
import chai from 'chai';

const { expect } = chai;

describe('createReducer', () => {
  it('should pass', () => {
    const foo = 1;
    expect(foo).to.eql(1);
  });
});
